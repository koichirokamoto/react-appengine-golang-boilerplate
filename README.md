# Boilerplate of React on Google App Engine for Go

## How to

* edit application field in `app.yaml`.

* build javascript


    $ npm run build

* try to execute command below.


    $ appcfg.py -A [your project id] -V v1 update [this diractory]
